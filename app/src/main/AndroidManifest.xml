<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="me.qidonk.footballapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:hardwareAccelerated="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:largeHeap="true"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".main.DetailMatchActivity">
        </activity>
        <activity android:name=".main.PlayerDetailActivity">
        </activity>
        <activity android:name=".main.SearchMatchActivity">
        </activity>
        <activity android:name=".main.SearchTeamActivity">
        </activity>
        <activity android:name=".main.TeamDetailActivity">
        </activity>
    </application>

</manifest>